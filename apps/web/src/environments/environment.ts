export const environment = {
  /** Empty = same-origin `/v1/*` (see `proxy.conf.json` + `ng serve`). Set full URL if you prefer cross-origin without a proxy. */
  apiUrl: '',
  lmsApiUrl: 'http://localhost:8081',
  production: false,
};
